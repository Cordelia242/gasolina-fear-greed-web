const latestUrl='./public/data/latest.json';const $=s=>document.querySelector(s);const fmt=new Intl.NumberFormat('es-BO');let latest=null,currentFilter='all',saldosRecords=null,dialogRecords=[],currentDialogStation=null,currentRange='5h';
let chartSeries={index:true,balance:true,volume:true};let lastChartData=null;
const SALDOS_DAYS_BACK=2;
const STATION_CHART_HOURS=5;
const rangeHours={'5h':5,'10h':10,'1d':24};
const fuelStateLabel=s=>({CRITICO:'CRÍTICO',ESCASEZ:'ESCASEZ',NORMAL:'NORMAL',ABUNDANCIA:'ABUNDANCIA',SATURADO:'SATURADO'})[s]||s||'—';
const pressureStateLabel=s=>({SIN_PRESION:'SIN PRESIÓN',DEMANDA_BAJA:'DEMANDA BAJA',EQUILIBRIO:'EQUILIBRIO',PRESION_ALTA:'PRESIÓN ALTA',PRESION_EXTREMA:'PRESIÓN EXTREMA',BASELINE_BUILDING:'BASELINE EN CONSTRUCCIÓN'})[s]||s||'—';
function localDateString(date=new Date()){const p=new Intl.DateTimeFormat('en-CA',{timeZone:'America/La_Paz',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(date),x=Object.fromEntries(p.map(v=>[v.type,v.value]));return `${x.year}-${x.month}-${x.day}`}
function dateOffsetString(daysAgo){return localDateString(new Date(Date.now()-daysAgo*86400000))}
async function getJSON(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`${r.status} ${url}`);return r.json()}
function esc(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}

const rootStyle=getComputedStyle(document.documentElement);
const FUEL_STATE_ORDER=['CRITICO','ESCASEZ','NORMAL','ABUNDANCIA','SATURADO'];
const FUEL_STATE_COLORS=Object.fromEntries(FUEL_STATE_ORDER.map(s=>[s,rootStyle.getPropertyValue(`--s-${s.toLowerCase()}`).trim()]));
const PRESSURE_STATE_ORDER=['SIN_PRESION','DEMANDA_BAJA','EQUILIBRIO','PRESION_ALTA','PRESION_EXTREMA'];
const PRESSURE_STATE_COLORS={SIN_PRESION:FUEL_STATE_COLORS.SATURADO,DEMANDA_BAJA:FUEL_STATE_COLORS.ABUNDANCIA,EQUILIBRIO:FUEL_STATE_COLORS.NORMAL,PRESION_ALTA:FUEL_STATE_COLORS.ESCASEZ,PRESION_EXTREMA:FUEL_STATE_COLORS.CRITICO};
const STATE_ORDER=PRESSURE_STATE_ORDER;
const STATE_COLORS=PRESSURE_STATE_COLORS;
const stateLabel=pressureStateLabel;
const PANEL_COLOR=rootStyle.getPropertyValue('--panel').trim();
const VOLUME_COLOR=rootStyle.getPropertyValue('--volume').trim();
const VOLUME_IN_COLOR=rootStyle.getPropertyValue('--volume-in').trim();
const VOLUME_OUT_COLOR=rootStyle.getPropertyValue('--volume-out').trim();
const BALANCE_COLOR=rootStyle.getPropertyValue('--balance-line').trim();
const ZONE_DIVIDER_COLOR=rootStyle.getPropertyValue('--zone-divider').trim();
function compactLiters(v){const n=Number(v)||0;if(n>=1000)return `${(n/1000).toFixed(n>=10000?0:1)}K L`;return `${Math.round(n)} L`}
const clampScore=v=>Math.max(0,Math.min(100,Number(v)||0));
const scoreToState=v=>PRESSURE_STATE_ORDER[Math.min(4,Math.floor(clampScore(v)/20))];
const scoreColor=v=>PRESSURE_STATE_COLORS[scoreToState(v)];
const fuelScoreColor=v=>FUEL_STATE_COLORS[FUEL_STATE_ORDER[Math.min(4,Math.floor(clampScore(v)/20))]];
function renderGauge(score){const numeric=score===null||score===undefined||score===''?NaN:Number(score);if(!Number.isFinite(numeric)){$('#gaugeSvg').innerHTML=`<path d="M 20 100 A 80 80 0 0 1 180 100" fill="none" stroke="#3a3542" stroke-width="14" stroke-linecap="round"/>`;return}const s=clampScore(numeric),cx=100,cy=100,r=80,angle=(180-(s/100)*180)*Math.PI/180,mx=(cx+r*Math.cos(angle)).toFixed(1),my=(cy-r*Math.sin(angle)).toFixed(1);$('#gaugeSvg').innerHTML=`<defs><linearGradient id="gaugeGrad" x1="0%" y1="0%" x2="100%" y2="0%"><stop offset="0%" stop-color="${PRESSURE_STATE_COLORS.SIN_PRESION}"/><stop offset="25%" stop-color="${PRESSURE_STATE_COLORS.DEMANDA_BAJA}"/><stop offset="50%" stop-color="${PRESSURE_STATE_COLORS.EQUILIBRIO}"/><stop offset="75%" stop-color="${PRESSURE_STATE_COLORS.PRESION_ALTA}"/><stop offset="100%" stop-color="${PRESSURE_STATE_COLORS.PRESION_EXTREMA}"/></linearGradient></defs><path d="M ${cx-r} ${cy} A ${r} ${r} 0 0 1 ${cx+r} ${cy}" fill="none" stroke="url(#gaugeGrad)" stroke-width="14" stroke-linecap="round"/><circle cx="${mx}" cy="${my}" r="8" fill="${PANEL_COLOR}" stroke="#fff" stroke-width="3"/>`}
function renderGlobal(d){const g=d.global||{},inventory=g.inventory||{},pressure=g.pressure||{},hasPressure=Number.isFinite(pressure.score),s=hasPressure?clampScore(pressure.score):null;$('#globalScore').textContent=hasPressure?Math.round(s):'--';renderGauge(s);const stateEl=$('#globalState');[...FUEL_STATE_ORDER,...PRESSURE_STATE_ORDER,'BASELINE_BUILDING'].forEach(st=>stateEl.classList.remove('state-'+st));stateEl.textContent=pressureStateLabel(pressure.state||'BASELINE_BUILDING');if(pressure.state)stateEl.classList.add('state-'+pressure.state);$('#totalLiters').textContent=`${fmt.format(inventory.totalLiters||0)} L`;$('#availableStations').textContent=inventory.stationsAvailable??0;$('#totalStations').textContent=inventory.stationsTotal??d.stations?.length??0;const dt=new Date(d.scrapedAt);$('#updatedAt').textContent=Number.isNaN(dt.getTime())?'Actualización reciente':`Actualizado ${new Intl.DateTimeFormat('es-BO',{dateStyle:'short',timeStyle:'short'}).format(dt)}`}
function stationRecordsInRange(key,hours){const cutoff=Date.now()-hours*3600*1000;return (saldosRecords||[]).filter(r=>r.station===key&&new Date(r.scrapedAt).getTime()>=cutoff).sort((a,b)=>new Date(a.scrapedAt)-new Date(b.scrapedAt))}
function stationSparkline(key,color){const records=stationRecordsInRange(key,STATION_CHART_HOURS),liters=records.map(r=>Number(r.liters||0)),w=280,h=52,p=4;
if(liters.length<2)return `<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><text x="50%" y="50%" text-anchor="middle" dominant-baseline="middle" font-size="10" fill="#6f6a7d" font-family="'IBM Plex Mono',ui-monospace,monospace">Sin histórico de 5h</text></svg>`;
const lo=0,hi=Math.max(...liters),span=(hi-lo)||1,pts=liters.map((v,i)=>[p+i*((w-p*2)/(liters.length-1)),h-p-((v-lo)/span)*(h-p*2)]),d=pts.map((pt,i)=>`${i?'L':'M'}${pt[0].toFixed(1)},${pt[1].toFixed(1)}`).join(' '),areaD=`${d} L${pts.at(-1)[0].toFixed(1)},${h-p} L${pts[0][0].toFixed(1)},${h-p} Z`,last=pts.at(-1);
return `<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><path d="${areaD}" fill="${color}" fill-opacity="0.14" stroke="none"></path><path d="${d}" fill="none" stroke="${color}" stroke-width="2" vector-effect="non-scaling-stroke"></path><circle cx="${last[0].toFixed(1)}" cy="${last[1].toFixed(1)}" r="3" fill="${color}"></circle></svg>`}
function stationConsumptionLabel(key){const records=stationRecordsInRange(key,1);if(records.length<2)return 'Sin datos (1h)';const first=Number(records[0].liters||0),last=Number(records.at(-1).liters||0),delta=last-first;if(delta>0)return `Recarga: +${fmt.format(delta)} L (1h)`;if(delta<0)return `~${fmt.format(Math.abs(delta))} L gastados (1h)`;return 'Sin cambios (1h)'}
function stationCard(s){const fuel=s.fuelLevel||{score:0,state:'CRITICO'},color=fuelScoreColor(fuel.score);return `<article class="card station state-${fuel.state}" data-key="${esc(s.key)}"><div class="station-top"><div><h3>${esc(s.name)}</h3><div class="address">${esc(s.address||'Dirección no disponible')}</div></div><div class="station-score">${Math.round(fuel.score??0)} · ${fuelStateLabel(fuel.state)}</div></div><div class="station-main"><strong>${fmt.format(s.liters||0)}</strong><span>litros</span></div><div class="station-chart">${stationSparkline(s.key,color)}</div><div class="station-meta"><span>Nivel: ${fuelStateLabel(fuel.state)}</span><span>${esc(stationConsumptionLabel(s.key))}</span></div></article>`}
function renderStations(){const all=[...(latest?.stations||[])].sort((a,b)=>(b.liters||0)-(a.liters||0)),v=all.filter(s=>currentFilter==='available'?(s.liters||0)>0:currentFilter==='critical'?(s.liters||0)===0:true);$('#stationsGrid').innerHTML=v.length?v.map(stationCard).join(''):'<div class="empty">No hay surtidores para este filtro.</div>'}
function svgLineChart(svg,values,{min,max,times,detailEl,formatValue,color='#8b7cd8'}={}){svg.onclick=null;svg.style.cursor='';if(values.length<2){svg.innerHTML='<text x="50%" y="50%" text-anchor="middle" fill="#6f6a7d" font-size="13" font-family="ui-monospace,monospace">Aún no hay suficiente histórico</text>';if(detailEl)detailEl.textContent='Aún no hay suficiente histórico para mostrar detalle.';return false}
const w=600,h=220,p=14,lo=min??Math.min(...values),hi=max??Math.max(...values),span=(hi-lo)||1,pts=values.map((v,i)=>[p+i*((w-p*2)/(values.length-1)),h-p-((v-lo)/span)*(h-p*2)]),d=pts.map((pt,i)=>`${i?'L':'M'}${pt[0].toFixed(1)},${pt[1].toFixed(1)}`).join(' '),areaD=`${d} L${pts.at(-1)[0].toFixed(1)},${h-p} L${pts[0][0].toFixed(1)},${h-p} Z`,last=pts.at(-1);
svg.innerHTML=`<path d="${areaD}" fill="${color}" fill-opacity="0.14" stroke="none"></path><line class="chart-guide" x1="${last[0]}" y1="${p}" x2="${last[0]}" y2="${h-p}" stroke="#f4b74e" stroke-width="1" stroke-dasharray="3,3" style="display:none"></line><path d="${d}" fill="none" stroke="${color}" stroke-width="2" vector-effect="non-scaling-stroke"></path><circle cx="${last[0]}" cy="${last[1]}" r="3" fill="${color}"></circle><circle class="chart-marker" r="5" fill="none" stroke="#f4b74e" stroke-width="2" style="display:none"></circle>`;
if(detailEl)detailEl.textContent='Toca el gráfico para ver el detalle de un momento.';
if(!times)return true;
const guide=svg.querySelector('.chart-guide'),marker=svg.querySelector('.chart-marker');
svg.style.cursor='pointer';
svg.onclick=evt=>{const rect=svg.getBoundingClientRect(),svgX=(evt.clientX-rect.left)*(w/rect.width),step=(w-p*2)/(values.length-1),idx=Math.max(0,Math.min(values.length-1,Math.round((svgX-p)/step))),pt=pts[idx];guide.setAttribute('x1',pt[0]);guide.setAttribute('x2',pt[0]);guide.style.display='';marker.setAttribute('cx',pt[0]);marker.setAttribute('cy',pt[1]);marker.style.display='';if(detailEl){const t=times[idx],timeLabel=t?new Intl.DateTimeFormat('es-BO',{dateStyle:'short',timeStyle:'short'}).format(new Date(t)):`Punto ${idx+1}`,valueLabel=formatValue?formatValue(values[idx]):values[idx];detailEl.textContent=`${timeLabel} · ${valueLabel}`}};
return true}
function drawZoneChart(svg,scores,times,tooltipEl,sold,liters,seriesEnabled={index:true,balance:true,volume:true}){svg.onclick=null;svg.style.cursor='';
const wrapEl=svg.parentElement;
if(tooltipEl)tooltipEl.style.display='none';
const w=Math.max(280,Math.round(svg.getBoundingClientRect().width))||600,h=Math.max(160,Math.round(svg.getBoundingClientRect().height))||220;
svg.setAttribute('viewBox',`0 0 ${w} ${h}`);
if(scores.length<2){svg.innerHTML=`<text x="${w/2}" y="${h/2}" text-anchor="middle" fill="#6f6a7d" font-size="13" font-family="ui-monospace,monospace">Aún no hay suficiente histórico</text>`;return}
const pL=44,pR=54,pT=14,pB=18,plotW=w-pL-pR,plotH=h-pT-pB,bandH=plotH/5;
const pts=scores.map((v,i)=>[pL+i*(plotW/(scores.length-1)),h-pB-(clampScore(v)/100)*plotH]);
const parts=[];
for(let i=0;i<5;i++){const y=pT+i*bandH,state=STATE_ORDER[4-i],color=STATE_COLORS[state],isEdge=i===0||i===4;if(isEdge)parts.push(`<rect x="${pL}" y="${y.toFixed(1)}" width="${plotW}" height="${bandH.toFixed(1)}" fill="${color}" fill-opacity="0.1"></rect>`);if(i>0)parts.push(`<line x1="${pL}" y1="${y.toFixed(1)}" x2="${w-pR}" y2="${y.toFixed(1)}" stroke="${ZONE_DIVIDER_COLOR}" stroke-width="1" stroke-dasharray="2,4"></line>`);parts.push(`<text x="${(w-pR+3).toFixed(1)}" y="${(y-3).toFixed(1)}" text-anchor="start" font-size="8" font-family="'IBM Plex Mono',ui-monospace,monospace" fill="#6f6a7d" opacity="0.8">${100-i*20}</text>`);parts.push(`<text x="${(w-2).toFixed(1)}" y="${(y+bandH/2+3).toFixed(1)}" text-anchor="end" font-size="9" font-family="'IBM Plex Mono',ui-monospace,monospace" fill="${color}" opacity="0.85">${stateLabel(state)}</text>`)}
parts.push(`<text x="${(w-pR+3).toFixed(1)}" y="${(h-pB-3).toFixed(1)}" text-anchor="start" font-size="8" font-family="'IBM Plex Mono',ui-monospace,monospace" fill="#6f6a7d" opacity="0.8">0</text>`);
const hasBalance=Array.isArray(liters)&&liters.length===scores.length,showBalance=hasBalance&&seriesEnabled.balance;
if(showBalance){const minL=Math.min(...liters),maxL=Math.max(...liters),spanL=(maxL-minL)||1,balPts=liters.map((v,i)=>[pts[i][0],h-pB-((v-minL)/spanL)*plotH]),balD=balPts.map((pt,i)=>`${i?'L':'M'}${pt[0].toFixed(1)},${pt[1].toFixed(1)}`).join(' '),lastBal=balPts.at(-1);
for(let i=0;i<=5;i++){const y=pT+i*bandH,val=maxL-i*(spanL/5);parts.push(`<text x="${(pL-4).toFixed(1)}" y="${(y-3).toFixed(1)}" text-anchor="end" font-size="8" font-family="'IBM Plex Mono',ui-monospace,monospace" fill="${BALANCE_COLOR}" opacity="0.7">${compactLiters(val)}</text>`)}
parts.push(`<path d="${balD}" fill="none" stroke="${BALANCE_COLOR}" stroke-width="1.5" stroke-opacity="0.55" stroke-linecap="round" vector-effect="non-scaling-stroke"></path>`);
parts.push(`<circle cx="${lastBal[0].toFixed(1)}" cy="${lastBal[1].toFixed(1)}" r="3" fill="${BALANCE_COLOR}"></circle>`)}
const hasVolume=Array.isArray(sold)&&sold.length===scores.length,showVolume=hasVolume&&seriesEnabled.volume;
if(showVolume){const volFrac=0.22,maxAbs=Math.max(1,...sold.map(v=>Math.abs(v))),baseY=h-pB,barZoneH=volFrac*plotH,barW=Math.max(1,(plotW/(scores.length-1))*0.92);
sold.forEach((v,i)=>{const bh=(Math.abs(v)/maxAbs)*barZoneH;if(bh<=0)return;const x=pts[i][0]-barW/2,y=baseY-bh,color=v>=0?VOLUME_IN_COLOR:VOLUME_OUT_COLOR;parts.push(`<rect x="${x.toFixed(1)}" y="${y.toFixed(1)}" width="${barW.toFixed(1)}" height="${bh.toFixed(1)}" fill="${color}" fill-opacity="0.55"></rect>`)})}
const tickCount=Math.min(5,times.length);
for(let i=0;i<tickCount;i++){const idx=Math.round(i*(times.length-1)/(tickCount-1||1)),x=pts[idx][0],label=times[idx]?new Intl.DateTimeFormat('es-BO',{day:'2-digit',month:'short'}).format(new Date(times[idx])):'',anchor=i===0?'start':i===tickCount-1?'end':'middle';parts.push(`<text x="${x.toFixed(1)}" y="${h-4}" text-anchor="${anchor}" font-size="9" font-family="'IBM Plex Mono',ui-monospace,monospace" fill="#6f6a7d">${esc(label)}</text>`)}
if(seriesEnabled.index){for(let i=1;i<pts.length;i++){const color=scoreColor((scores[i-1]+scores[i])/2);parts.push(`<path d="M${pts[i-1][0].toFixed(1)},${pts[i-1][1].toFixed(1)} L${pts[i][0].toFixed(1)},${pts[i][1].toFixed(1)}" fill="none" stroke="${color}" stroke-width="2" stroke-linecap="round" vector-effect="non-scaling-stroke"></path>`)}
const last=pts.at(-1),lastColor=scoreColor(scores.at(-1));
parts.push(`<circle cx="${last[0].toFixed(1)}" cy="${last[1].toFixed(1)}" r="4" fill="${lastColor}" stroke="${PANEL_COLOR}" stroke-width="2"></circle>`);
parts.push(`<rect x="${(last[0]+6).toFixed(1)}" y="${(last[1]-9).toFixed(1)}" width="30" height="18" rx="4" fill="${lastColor}"></rect>`);
parts.push(`<text x="${(last[0]+21).toFixed(1)}" y="${(last[1]+4).toFixed(1)}" text-anchor="middle" font-size="11" font-weight="700" font-family="'IBM Plex Mono',ui-monospace,monospace" fill="${PANEL_COLOR}">${Math.round(scores.at(-1))}</text>`)}
parts.push(`<line class="chart-guide" x1="${pts.at(-1)[0].toFixed(1)}" y1="${pT}" x2="${pts.at(-1)[0].toFixed(1)}" y2="${h-pB}" stroke="#f4b74e" stroke-width="1" stroke-dasharray="3,3" style="display:none"></line>`);
parts.push(`<circle class="chart-marker" r="5" fill="none" stroke="#f4b74e" stroke-width="2" style="display:none"></circle>`);
svg.innerHTML=parts.join('');
const guide=svg.querySelector('.chart-guide'),marker=svg.querySelector('.chart-marker');
svg.style.cursor='pointer';
svg.onclick=evt=>{const rect=svg.getBoundingClientRect(),svgX=(evt.clientX-rect.left)*(w/rect.width),step=plotW/(pts.length-1),idx=Math.max(0,Math.min(pts.length-1,Math.round((svgX-pL)/step))),pt=pts[idx];guide.setAttribute('x1',pt[0]);guide.setAttribute('x2',pt[0]);guide.style.display='';if(seriesEnabled.index){marker.setAttribute('cx',pt[0]);marker.setAttribute('cy',pt[1]);marker.setAttribute('stroke',scoreColor(scores[idx]));marker.style.display=''}else{marker.style.display='none'}
if(tooltipEl)showChartTooltip(tooltipEl,wrapEl,pt[0],{time:times[idx],score:scores[idx],liters:showBalance?liters[idx]:null,sold:showVolume?sold[idx]:null},seriesEnabled)}}
function showChartTooltip(tooltipEl,wrapEl,xPos,data,seriesEnabled){
const dt=data.time?new Date(data.time):null,valid=dt&&!Number.isNaN(dt.getTime());
const dateLabel=valid?new Intl.DateTimeFormat('es-BO',{day:'2-digit',month:'2-digit',year:'numeric'}).format(dt):'—';
const timeLabel=valid?new Intl.DateTimeFormat('es-BO',{hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(dt):'';
const rows=[];
if(seriesEnabled.index&&Number.isFinite(data.score)){const color=scoreColor(data.score);rows.push(`<li><span class="tooltip-dot" style="background:${color}"></span><span class="tooltip-label">Índice de presión</span><strong>${Math.round(data.score)} · ${esc(stateLabel(scoreToState(data.score)))}</strong></li>`)}
if(seriesEnabled.balance&&data.liters!=null)rows.push(`<li><span class="tooltip-dot" style="background:${BALANCE_COLOR}"></span><span class="tooltip-label">Saldo total</span><strong>${fmt.format(Math.round(data.liters))} L</strong></li>`);
if(seriesEnabled.volume&&data.sold!=null){const color=data.sold>=0?VOLUME_IN_COLOR:VOLUME_OUT_COLOR,label=data.sold>0?`+${fmt.format(Math.round(data.sold))} L`:data.sold<0?`-${fmt.format(Math.round(Math.abs(data.sold)))} L`:'0 L';rows.push(`<li><span class="tooltip-dot" style="background:${color}"></span><span class="tooltip-label">Volumen</span><strong>${label}</strong></li>`)}
tooltipEl.innerHTML=`<div class="tooltip-date"><strong>${esc(dateLabel)}</strong><span>${esc(timeLabel)}</span></div><ul class="tooltip-rows">${rows.join('')}</ul>`;
tooltipEl.style.visibility='hidden';tooltipEl.style.display='block';tooltipEl.style.left='0px';tooltipEl.style.top='0px';
const wrapRect=wrapEl.getBoundingClientRect(),tw=tooltipEl.offsetWidth,th=tooltipEl.offsetHeight,margin=8;
const left=Math.max(margin,Math.min(xPos-tw/2,wrapRect.width-tw-margin)),top=Math.max(margin,Math.min(margin,wrapRect.height-th-margin));
tooltipEl.style.left=`${left}px`;tooltipEl.style.top=`${top}px`;tooltipEl.style.visibility='visible'}
function downsample(scores,times,max){if(scores.length<=max)return{scores,times};const stride=Math.ceil(scores.length/max),outS=[],outT=[];for(let i=0;i<scores.length;i+=stride){outS.push(scores[i]);outT.push(times[i])}if(outT.at(-1)!==times.at(-1)){outS.push(scores.at(-1));outT.push(times.at(-1))}return{scores:outS,times:outT}}
function downsampleWithVolume(scores,sold,liters,times,max){if(scores.length<=max)return{scores,sold,liters,times};const stride=Math.ceil(scores.length/max),outS=[],outV=[],outL=[],outT=[];for(let i=0;i<scores.length;i+=stride){const end=Math.min(i+stride,scores.length);outS.push(scores[i]);outT.push(times[i]);outL.push(liters[i]);let sum=0;for(let j=i;j<end;j++)sum+=sold[j];outV.push(sum)}if(outT.at(-1)!==times.at(-1)){outS.push(scores.at(-1));outT.push(times.at(-1));outV.push(sold.at(-1));outL.push(liters.at(-1))}return{scores:outS,sold:outV,liters:outL,times:outT}}
async function fetchHistoryDay(dateStr){try{return await getJSON(`./public/data/history/${dateStr}.json`)}catch{return null}}
async function loadHistoryRange(maxDays){const cap=Math.min(maxDays,400),missGrace=20;let hits=0,misses=0;const files=[];
for(let start=0;start<cap;start+=10){const batch=[];for(let i=start;i<Math.min(start+10,cap);i++)batch.push(i);const results=await Promise.all(batch.map(i=>fetchHistoryDay(dateOffsetString(i))));let batchHits=0;results.forEach(f=>{if(f){files.push(f);batchHits++;hits++}else misses++});if(batchHits===0&&((hits>0&&misses>=missGrace)||(hits===0&&misses>=missGrace)))break}
files.sort((a,b)=>new Date(a.date)-new Date(b.date));
const snapshots=files.flatMap(f=>f.snapshots||[]);
snapshots.sort((a,b)=>new Date(a.scrapedAt)-new Date(b.scrapedAt));
return snapshots}
const rangeCache={};
function getRangeSnapshots(maxDays,key){if(!rangeCache[key])rangeCache[key]=loadHistoryRange(maxDays);return rangeCache[key]}
async function renderTrendChart(rangeKey){const days=rangeKey==='7d'?7:rangeKey==='30d'?30:400;const snaps=await getRangeSnapshots(days,rangeKey);const points=snaps.map(s=>({score:Number.isFinite(s.global?.pressure?.score)?clampScore(s.global.pressure.score):null,liters:Number(s.global?.inventory?.totalLiters||0),time:s.scrapedAt}));if(latest&&(!points.length||new Date(latest.scrapedAt)>new Date(points.at(-1).time)))points.push({score:Number.isFinite(latest.global?.pressure?.score)?clampScore(latest.global.pressure.score):null,liters:Number(latest.global?.inventory?.totalLiters||0),time:latest.scrapedAt});const pressurePoints=points.filter(p=>Number.isFinite(p.score));if(!pressurePoints.length){lastChartData={scores:[],sold:[],liters:[],times:[]};drawZoneChart($('#trendChart'),[],[],$('#trendChartTooltip'),[],[],chartSeries);$('#trendDelta').textContent='BASELINE';return}const rawScores=pressurePoints.map(p=>p.score),rawTimes=pressurePoints.map(p=>p.time),rawLiters=pressurePoints.map(p=>p.liters),rawDelta=rawLiters.map((v,i)=>i===0?0:v-rawLiters[i-1]);const{scores,sold,liters,times}=downsampleWithVolume(rawScores,rawDelta,rawLiters,rawTimes,180);lastChartData={scores,sold,liters,times};drawZoneChart($('#trendChart'),scores,times,$('#trendChartTooltip'),sold,liters,chartSeries);if(scores.length<2){$('#trendDelta').textContent='—';return}const delta=scores.at(-1)-scores[0];$('#trendDelta').textContent=`${delta>=0?'+':''}${delta.toFixed(1)} pts`}
async function fetchSaldosDay(dateStr){try{return await getJSON(`./public/data/saldos/${dateStr}.json`)}catch{return null}}
async function getSaldosRecords(){if(saldosRecords)return saldosRecords;const days=await Promise.all(Array.from({length:SALDOS_DAYS_BACK},(_,i)=>fetchSaldosDay(dateOffsetString(i))));saldosRecords=days.filter(Boolean).flatMap(d=>d.records||[]);return saldosRecords}
function renderStationChart(){const hours=rangeHours[currentRange]??STATION_CHART_HOURS,cutoff=Date.now()-hours*3600*1000,records=dialogRecords.filter(r=>new Date(r.scrapedAt).getTime()>=cutoff),liters=records.map(r=>Number(r.liters||0)),times=records.map(r=>r.scrapedAt),color=currentDialogStation?fuelScoreColor(currentDialogStation.fuelLevel?.score):'#8b7cd8';svgLineChart($('#stationChart'),liters,{min:0,times,detailEl:$('#stationChartDetail'),formatValue:v=>`${fmt.format(v)} L`,color})}
async function openStationDialog(key){const station=(latest?.stations||[]).find(s=>s.key===key);if(!station)return;currentDialogStation=station;currentRange='5h';document.querySelectorAll('.range-filters .filter').forEach(x=>x.classList.toggle('active',x.dataset.range==='5h'));$('#dialogName').textContent=station.name;$('#dialogAddress').textContent=station.address||'Dirección no disponible';$('#dialogLiters').textContent=`${fmt.format(station.liters||0)} L`;const fuel=station.fuelLevel||{score:0,state:'CRITICO'},pressure=station.pressure||{score:null,state:'BASELINE_BUILDING'};$('#dialogFuelLevel').textContent=`${Math.round(fuel.score)} · ${fuelStateLabel(fuel.state)}`;$('#dialogPressure').textContent=Number.isFinite(pressure.score)?`${Math.round(pressure.score)} · ${pressureStateLabel(pressure.state)}`:pressureStateLabel(pressure.state||'BASELINE_BUILDING');$('#stationChart').innerHTML='';dialogRecords=[];$('#stationDialog').showModal();dialogRecords=(await getSaldosRecords()).filter(r=>r.station===key).sort((a,b)=>new Date(a.scrapedAt)-new Date(b.scrapedAt));renderStationChart()}
async function init(){try{latest=await getJSON(latestUrl);renderGlobal(latest);await getSaldosRecords();renderStations();renderTrendChart('7d')}catch(e){console.error(e);$('#updatedAt').textContent='No se pudieron cargar los datos';$('#liveDot').style.background='#e2807a';$('#stationsGrid').innerHTML='<div class="empty">No se encontró public/data/latest.json. El dashboard funcionará cuando n8n publique el primer snapshot.</div>'}}
let currentChartRange='7d';
document.addEventListener('click',e=>{const tt=$('#trendChartTooltip');if(tt&&tt.style.display==='block'&&!e.target.closest('#trendChartWrap'))tt.style.display='none';const leg=e.target.closest('#chartLegend .legend-item');if(leg){const key=leg.dataset.series;chartSeries[key]=!chartSeries[key];leg.classList.toggle('disabled',!chartSeries[key]);leg.classList.toggle('active',chartSeries[key]);leg.setAttribute('aria-pressed',String(chartSeries[key]));if(lastChartData)drawZoneChart($('#trendChart'),lastChartData.scores,lastChartData.times,$('#trendChartTooltip'),lastChartData.sold,lastChartData.liters,chartSeries);return}const cr=e.target.closest('#chartRangeButtons .filter');if(cr){document.querySelectorAll('#chartRangeButtons .filter').forEach(x=>x.classList.remove('active'));cr.classList.add('active');currentChartRange=cr.dataset.range;renderTrendChart(currentChartRange);return}const r=e.target.closest('.range-filters .filter');if(r){document.querySelectorAll('.range-filters .filter').forEach(x=>x.classList.remove('active'));r.classList.add('active');currentRange=r.dataset.range;renderStationChart();return}const b=e.target.closest('.filters:not(.range-filters) .filter');if(b){document.querySelectorAll('.filters:not(.range-filters) .filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');currentFilter=b.dataset.filter;renderStations();return}const card=e.target.closest('.station[data-key]');if(card)openStationDialog(card.dataset.key)});
$('#dialogClose').addEventListener('click',()=>$('#stationDialog').close());
$('#stationDialog').addEventListener('click',e=>{if(e.target===$('#stationDialog'))$('#stationDialog').close()});
let chartResizeTimer=null;
window.addEventListener('resize',()=>{clearTimeout(chartResizeTimer);chartResizeTimer=setTimeout(()=>{if(latest)renderTrendChart(currentChartRange)},200)});
init();
